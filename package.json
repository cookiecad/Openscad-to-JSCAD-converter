{
  "name": "openscad-to-jscad-converter",
  "scripts": {
    "lint": "standard --fix --env mocha",
    "preinstall": "npx only-allow pnpm"

  },
  "devDependencies": {
    "standard": "^17.1.0"
  }
}
